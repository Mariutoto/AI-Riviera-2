# Sorties du pilote

Ce dossier accueillera plus tard :

- `inventory.json` : inventaire recalculé ;
- `embedding_inputs.jsonl` : textes prêts à envoyer, sans vecteurs ;
- `embedding_inputs.html` : lecture humaine de tous les inputs ;
- `retrieval_report.html` : résultats des tests de recherche ;
- `embeddings/` : fichiers temporaires ignorés par Git.

Aucune sortie d'embedding n'est présente pour le moment.
