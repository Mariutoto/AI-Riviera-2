# Metadata Templates

These files are reference shapes used by `app.metadata_enrichment` to label the
metadata template associated with each document category.

They describe the enriched metadata targets. Generated audit reports and raw
sample files were removed from the repository.
